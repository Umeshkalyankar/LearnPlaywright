// while loop - A while loop keeps running as long as the condition is true.

// let attempt = 0;  // init
// while (attempt < 3) {  // condition
//     console.log(attempt);
//     attempt++;  // updation
// }


let retry = 1;

while (retry <= 3) {
    console.log(`Attempt ${retry}`);
    retry++;
}