
let fruits = []; // empty []

let fruits_fresh = ["apple", "mango", "banana"];
//length = 3, index - 0,1,2


let arr = [10, 20, 30, 40]; // 0-3: 4
console.log(arr[0]);
console.log(arr[3]);
console.log(arr[4]);  // if not found - ouput will be 'undefined'


let testResults = ["pass", "fail", "pass", "skip"]; // real use like this

let mixed = [1, "hello", true, null] // JS arrays can hold any types