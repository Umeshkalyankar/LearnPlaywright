// Q. Suppose you have 10 test cases - but run only first 3 cases:
const tests = ["Login", "Search", "Checkout", "Profile", "Wishlist",];

let smokeTests = tests.slice(0, 3);
console.log(smokeTests);

