function getUserStatus() {
    //var status_code; variable created by JS Engine(optimize the code)
    console.log(status_code);
    var status_code = "Active";
    console.log(status_code);
}
getUserStatus();

//Note: var is function-scoped so function is hoisted to
//getUserStatus(), NOT the Global scope.