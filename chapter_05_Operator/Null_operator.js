
console.log(null >= 0); // null > 0 or null = 0
console.log(null === 0);

// nullish(??) - used in Automation (used in JavaScript)
let amul = null;
let milk_required = amul ?? "nandini milk";
console.log(milk_required);

// nullish -(?) - used in TypeScript
// val = "which milk? -> " + val;
// console.log(val);

