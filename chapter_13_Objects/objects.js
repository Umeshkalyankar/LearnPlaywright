// objects - collection of key-value pairs > stored in Hipped memory
// let obj = { key : value}
// objects are Dynamic in nature

let obj = {                        // obj is object reference
    name: "Umesh",                 // this are properties of object
    age: 29,                       // this are properties of object
    role: "Tester"                 // this are properties of object
};

// accessing object
console.log(obj.name);  // dot notation
console.log(obj["age"]); // bracket notation
console.log(obj.role);
